describe('binaryPlateau', function() {
    it('should find the largest binary plateau when given an integer', function() {
      var result = binaryPlateau( );
      expect(result).toEqual( );
    });
});